node_modules/
dist/
*.log
.env
.env.local
.DS_Store
# Local agent scratch (SDD briefs/reports/diffs). Tracked here — not just in
# .git/info/exclude — so no clone can accidentally publish internal notes.
.superpowers/
